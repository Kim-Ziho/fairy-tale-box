spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/c101test;
    username: ssafy
    password: 123
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #          show_sql: true  system.out으로 찍어냄.
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace